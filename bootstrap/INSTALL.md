# Source bootstrap

The complete Flutter source is stored in the numbered `source.part-*` files on this branch. The repository's one-time import workflows verify the archive checksum, extract it at the repository root, run the repository contract checks, remove this directory, and commit the resulting source tree.

Expected source archive SHA-256:

```text
fc941aed2a65fef1eea06b8f8bdd51905506f304de6c3c5b2440db8b9d227d1e
```
